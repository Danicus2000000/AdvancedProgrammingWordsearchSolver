#include "gridNode.h"
gridNode::gridNode(const char letterToAdd) : up(nullptr), down(nullptr), left(nullptr), right(nullptr), upRight(nullptr), upLeft(nullptr), downLeft(nullptr), downRight(nullptr), letter(letterToAdd)
{
}